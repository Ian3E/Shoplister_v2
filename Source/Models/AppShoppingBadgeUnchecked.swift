import Foundation

enum AppShoppingBadgeUnchecked {
    /// When **on**, the home-screen icon badge shows the count of **unchecked** shopping lines (resolved catalog items only).
    static let storageKey = "app.shoppingBadgeUncheckedCountEnabled"
}
